services:
  postgres:
    image: 'postgres:latest' # TODO: specify postgres version 16 here
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432'
